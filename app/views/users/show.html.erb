<head>
	<title>ESC PT - User Points</title>
	<%= stylesheet_link_tag "users" %>
</head>

<div class="col-padding-30 d-flex justify-content-center">
	<h1 class="page-header"> Your Points </h1> <br/>
</div>

<div class="col-card d-flex justify-content-center">
	<div class="card w-50">
		<div class="card-header text-center">
			<h1 class="card-title"><%=@user.first_name + ' ' + @user.last_name%></h1>
		</div>
		<div class="card-body">
			<div class="tab-content">
					<p class="content-paragraph">You have <%=@user.points%> point(s).</p>
			</div>
		</div>
	</div>
